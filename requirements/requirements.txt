# Основные библиотеки
requests         # Для работы с HTTP-запросами
beautifulsoup4   # Для парсинга HTML-страниц
#numpy            # Для научных вычислений
#pandas           # Для обработки данных
flask            # Минималистичный веб-фреймворк (если используется)
flask-wtf        # Для работы с формами в Flask (если используется)
wtforms          # Для работы с формами в Flask (если используется)
qrcode           # Для создания QR-кодов
flask-sqlalchemy  # Для работы с SQL-базами данных в Flask (если используется)
pyperclip        # Копирование в буфер обмена

# Зависимости для API или асинхронности
#fastapi        # Асинхронный веб-фреймворк (если используется)
#uvicorn          # Сервер для запуска FastAPI (если используется)

# Зависимости для баз данных
sqlalchemy        # Работа с SQL-базами данных
psycopg2-binary   # PostgreSQL драйвер (если используется)

# Зависимости для работы с файлами
openpyxl        # Работа с Excel-файлами
PyYAML            # Работа с YAML-файлами
pillow           # Работа с изображениями

# Обеспечение безопасности
python-dotenv    # Загрузка переменных окружения из .env файла
flask-bcrypt     # Хеширование паролей в Flask (если используется)
flask-login      # Аутентификация пользователей в Flask (если используется)